# Полная инструкция по странице доски задач (Task Board)

## 📋 Содержание
1. [Общее описание страницы](#общее-описание-страницы)
2. [Структура страницы](#структура-страницы)
3. [Верхняя панель (Header)](#верхняя-панель-header)
4. [Панель фильтров (левая боковая)](#панель-фильтров-левая-боковая)
5. [Основная область - Канвас задач](#основная-область---канвас-задач)
6. [Карточка задачи (Task Node)](#карточка-задачи-task-node)
7. [Панель настроек задачи](#панель-настроек-задачи)
8. [Контекстные меню](#контекстные-меню)
9. [Взаимодействия и жесты](#взаимодействия-и-жесты)
10. [Горячие клавиши](#горячие-клавиши)
11. [Визуальные эффекты и анимации](#визуальные-эффекты-и-анимации)
12. [Состояния элементов](#состояния-элементов)

---

## Общее описание страницы

**Страница доски задач** - это основной интерфейс для работы с задачами проекта. Представляет собой бесконечный канвас с возможностью размещения, перемещения и организации задач в свободном пространстве.

**URL:** `/board/[projectId]`

**Основные возможности:**
- ✅ Бесконечный канвас 5000x5000px
- ✅ Масштабирование от 30% до 200%
- ✅ Панорамирование (перемещение по канвасу)
- ✅ Создание, редактирование, удаление задач
- ✅ Перетаскивание задач
- ✅ Иерархические связи между задачами
- ✅ Фильтрация задач
- ✅ Реалтайм синхронизация изменений
- ✅ Отслеживание онлайн-пользователей

---

## Структура страницы

### Макет страницы (Layout)

```
┌─────────────────────────────────────────────────────────────┐
│ ВЕРХНЯЯ ПАНЕЛЬ (Header) - фиксированная, высота 64px       │
│ [←] [Название проекта] [Badges] ... [Участники] [⚙️] [Zoom]│
├─────┬───────────────────────────────────────────────────────┤
│     │                                                       │
│ ФИЛ │         ОСНОВНАЯ ОБЛАСТЬ - КАНВАС ЗАДАЧ              │
│ ТРЫ │         (бесконечное пространство)                    │
│     │                                                       │
│     │         [Задача 1]  [Задача 2]                       │
│     │              │            │                          │
│     │              └────────────┘                          │
│     │                                                       │
│     │         [Задача 3]                                   │
│     │                                                       │
└─────┴───────────────────────────────────────────────────────┘
```

**Размеры:**
- Верхняя панель: `height: 64px` (фиксированная)
- Панель фильтров: `width: 320px` (открыта) / `48px` (закрыта)
- Канвас: `width: 5000px`, `height: 5000px` (виртуальный размер)
- Область просмотра: `calc(100vh - 64px)` (высота экрана минус header)

---

## Верхняя панель (Header)

### Расположение и стили

**Позиция:** Фиксированная вверху страницы  
**Высота:** `64px` (py-3.5 = 14px padding)  
**Фон:** `bg-zinc-950/60 backdrop-blur-2xl`  
**Граница:** `border-b border-white/[0.08]`  
**Отступы:** `px-6 py-3.5`

### Левая часть Header

#### 1. Кнопка "Назад"

**Расположение:** Крайний левый элемент  
**Размеры:** `h-5 w-5` (иконка), `p-2` (padding)  
**Стили:**
```tsx
className="rounded-xl p-2 text-white/50 transition-all hover:bg-white/[0.06] hover:text-white"
```

**Функция:** Переход на страницу `/projects`  
**Иконка:** `ArrowLeft` (Lucide React)

---

#### 2. Название проекта

**Режим 1: Отображение (по умолчанию)**

**Структура:**
```tsx
<div className="flex items-center gap-2">
  <h1 className="text-xl font-semibold tracking-tight text-white">
    {projectName}
  </h1>
  {isOwner && (
    <button onClick={() => setIsEditingName(true)}>
      <Edit2 className="h-4 w-4" />
    </button>
  )}
</div>
```

**Стили:**
- Заголовок: `text-xl font-semibold tracking-tight text-white`
- Кнопка редактирования: `rounded-lg p-1.5 text-white/30 hover:bg-white/[0.06] hover:text-white/70`
- Иконка: `Edit2`, размер `h-4 w-4`

**Условия отображения:**
- Кнопка редактирования видна только владельцу проекта (`isOwner`)

---

**Режим 2: Редактирование**

**Структура:**
```tsx
<div className="flex items-center gap-2">
  <Input
    value={editedName}
    onChange={(e) => setEditedName(e.target.value)}
    onKeyDown={(e) => {
      if (e.key === "Enter") handleUpdateProjectName()
      if (e.key === "Escape") {
        setIsEditingName(false)
        setEditedName(projectName)
      }
    }}
    className="h-9 w-64 border-white/[0.12] bg-white/[0.04] text-white placeholder:text-white/40"
    autoFocus
  />
  <button onClick={handleUpdateProjectName}>
    <Check className="h-4 w-4 text-emerald-400" />
  </button>
</div>
```

**Стили поля ввода:**
- Размеры: `h-9 w-64`
- Граница: `border-white/[0.12]`
- Фон: `bg-white/[0.04]`
- Текст: `text-white`
- Плейсхолдер: `placeholder:text-white/40`

**Кнопка сохранения:**
- Стили: `rounded-lg p-2 text-emerald-400 hover:bg-emerald-500/10`
- Иконка: `Check`, размер `h-4 w-4`

**Горячие клавиши:**
- `Enter` - сохранить изменения
- `Escape` - отменить редактирование

---

#### 3. Badge "Публичный проект"

**Условие отображения:** `isPublic === true`

**Структура:**
```tsx
<span className="flex items-center gap-1.5 rounded-full bg-emerald-500/[0.15] px-3 py-1.5 text-xs font-medium text-emerald-400 ring-1 ring-emerald-500/20">
  <Globe className="h-3 w-3" />
  Публичный
</span>
```

**Стили:**
- Фон: `bg-emerald-500/[0.15]` (15% прозрачности)
- Текст: `text-emerald-400`
- Граница: `ring-1 ring-emerald-500/20`
- Скругление: `rounded-full`
- Отступы: `px-3 py-1.5`
- Размер текста: `text-xs font-medium`
- Иконка: `Globe`, размер `h-3 w-3`

---

#### 4. Badge "Только просмотр"

**Условие отображения:** `canEdit === false`

**Структура:**
```tsx
<span className="flex items-center gap-1.5 rounded-full bg-amber-500/[0.15] px-3 py-1.5 text-xs font-medium text-amber-400 ring-1 ring-amber-500/20">
  <Lock className="h-3 w-3" />
  Только просмотр
</span>
```

**Стили:**
- Фон: `bg-amber-500/[0.15]`
- Текст: `text-amber-400`
- Граница: `ring-1 ring-amber-500/20`
- Иконка: `Lock`, размер `h-3 w-3`

---

### Правая часть Header

#### 5. Выпадающий список участников (OnlineUsersDropdown)

**Расположение:** Справа от названия проекта

**Внешний вид (кнопка):**
```tsx
<Button className="flex items-center gap-2 rounded-xl border border-white/10 bg-white/5 px-3 py-1.5">
  <div className="flex -space-x-2">
    {/* Аватары онлайн пользователей (до 3) */}
  </div>
  {onlineMembers.length > 3 && (
    <span className="text-xs text-white/60">+{count}</span>
  )}
  <Users className="h-4 w-4 text-white/60" />
</Button>
```

**Стили кнопки:**
- Фон: `bg-white/5`
- Граница: `border border-white/10`
- Скругление: `rounded-xl`
- Отступы: `px-3 py-1.5`

**Аватары:**
- Размер: `h-6 w-6`
- Скругление: `rounded-full`
- Перекрытие: `-space-x-2` (отрицательный margin)
- Граница: `ring-2 ring-[rgb(15,23,42)]`
- Максимум отображается: 3 аватара
- Если больше 3: показывается `+N`

**Выпадающее меню:**

**Структура:**
```tsx
<DropdownMenuContent className="w-64 border-white/10 bg-slate-900/95 backdrop-blur-xl">
  <div className="px-2 py-2">
    <p className="text-xs font-semibold text-white/60">Участники проекта</p>
  </div>
  <DropdownMenuSeparator className="bg-white/10" />
  <div className="max-h-64 overflow-y-auto">
    {/* Список участников */}
  </div>
  {canInvite && (
    <>
      <DropdownMenuSeparator />
      <DropdownMenuItem onClick={copyInviteLink}>
        <LinkIcon /> Пригласить редактора
      </DropdownMenuItem>
    </>
  )}
</DropdownMenuContent>
```

**Элемент участника:**
```tsx
<DropdownMenuItem className="flex items-center gap-3 px-3 py-2">
  <div className="relative">
    {/* Аватар */}
    <div className="h-7 w-7 rounded-full" style={{backgroundColor: avatar_color}}>
      {avatar_image ? <img /> : <span>{display_name[0]}</span>}
    </div>
    {/* Индикатор онлайн */}
    {isOnline && (
      <div className="absolute bottom-0 right-0 h-2.5 w-2.5 rounded-full border-2 border-slate-900 bg-green-500" />
    )}
  </div>
  <div className="flex-1">
    <p className={isOnline ? "text-white" : "text-white/40"}>
      {display_name}
    </p>
    <p className="text-xs text-white/40">
      {role === "owner" ? "Владелец" : role === "editor" ? "Редактор" : "Наблюдатель"}
    </p>
  </div>
  {isOwner && !isCurrentUser && role !== "owner" && (
    <button onClick={() => removeMember(userId)}>
      <X className="h-3.5 w-3.5" />
    </button>
  )}
</DropdownMenuItem>
```

**Функции:**
- Отображение всех участников проекта
- Индикатор онлайн-статуса (зеленый кружок)
- Приглушенный вид для офлайн пользователей
- Удаление участника (только для владельца)
- Генерация пригласительной ссылки

**Индикатор онлайн:**
- Размер: `h-2.5 w-2.5`
- Цвет: `bg-green-500`
- Граница: `border-2 border-slate-900`
- Позиция: `absolute bottom-0 right-0`

---

#### 6. Кнопка настроек проекта (ProjectSettingsDialog)

**Расположение:** Справа от списка участников

**Кнопка:**
```tsx
<Button variant="ghost" size="icon" className="h-8 w-8 hover:bg-white/[0.06]">
  <Settings className="h-4 w-4" />
</Button>
```

**Условие отображения:** Только для владельца (`isOwner`)

**Диалог настроек:**

**Структура:**
```tsx
<DialogContent className="border-white/[0.12] bg-zinc-900/98 backdrop-blur-2xl sm:max-w-[440px]">
  <DialogHeader>
    <DialogTitle>Настройки проекта</DialogTitle>
    <DialogDescription>Измените название проекта и настройки видимости</DialogDescription>
  </DialogHeader>
  
  {/* Поле названия */}
  <Input value={name} onChange={(e) => setName(e.target.value)} />
  
  {/* Переключатель публичности */}
  <div className="flex items-center justify-between rounded-xl border border-white/[0.12] bg-white/[0.04] p-4">
    <div className="flex items-center gap-3">
      {publicProject ? <Globe /> : <Lock />}
      <div>
        <p>Публичный/Приватный проект</p>
        <p className="text-xs text-white/60">Описание</p>
      </div>
    </div>
    <Switch checked={publicProject} onCheckedChange={setPublicProject} />
  </div>
  
  {/* Код доступа (если публичный) */}
  {publicProject && (
    <div className="rounded-xl border border-blue-500/[0.25] bg-blue-500/[0.08] p-4">
      {/* Отображение/генерация кода */}
    </div>
  )}
  
  {/* Опасная зона */}
  <div className="rounded-xl border border-red-500/[0.25] bg-red-500/[0.08] p-4">
    <Button variant="destructive" onClick={handleDelete}>
      <Trash2 /> Удалить проект
    </Button>
  </div>
  
  <DialogFooter>
    <Button variant="outline" onClick={() => setOpen(false)}>Отмена</Button>
    <Button onClick={handleSave}>Сохранить</Button>
  </DialogFooter>
</DialogContent>
```

**Функции диалога:**
- Изменение названия проекта
- Переключение публичности проекта
- Генерация/отображение кода доступа
- Удаление кода доступа
- Удаление проекта

---

#### 7. Контрол масштабирования (Zoom Control)

**Расположение:** Крайний правый элемент

**Структура:**
```tsx
<div className="flex items-center gap-2 rounded-xl border border-white/[0.08] bg-white/[0.03] px-2 py-1">
  <button onClick={() => setScale(s => Math.min(2, s + 0.1))}>
    <ChevronUp className="h-4 w-4" />
  </button>
  <span className="min-w-[56px] text-center text-sm font-medium text-white/90">
    {Math.round(scale * 100)}%
  </span>
  <button onClick={() => setScale(s => Math.max(0.3, s - 0.1))}>
    <ChevronDown className="h-4 w-4" />
  </button>
</div>
```

**Стили:**
- Контейнер: `rounded-xl border border-white/[0.08] bg-white/[0.03] px-2 py-1`
- Кнопки: `rounded-lg p-1.5 text-white/70 hover:bg-white/[0.08] hover:text-white`
- Текст: `min-w-[56px] text-center text-sm font-medium text-white/90`

**Функции:**
- Увеличение масштаба: `+10%` (максимум 200%)
- Уменьшение масштаба: `-10%` (минимум 30%)
- Отображение текущего масштаба в процентах

**Альтернативный способ:** `Ctrl + колесо мыши` (см. раздел "Взаимодействия")

---

## Панель фильтров (левая боковая)

### Расположение

**Позиция:** Абсолютная, слева от канваса  
**Высота:** `calc(100vh - 64px)` (от header до низа экрана)  
**Ширина:** 
- Закрыта: `48px`
- Открыта: `320px`
- Анимация: `transition-all duration-300 ease-out`

**Z-index:** `z-50`

---

### Кнопка переключения

**Расположение:** Верхний левый угол панели

**Структура:**
```tsx
<button
  onClick={() => setIsFilterOpen(!isFilterOpen)}
  className="absolute left-0 top-4 z-10 flex h-10 w-10 items-center justify-center rounded-r-xl border border-l-0 border-white/[0.12] bg-zinc-900/95 text-white/70 backdrop-blur-xl transition-all hover:bg-zinc-800/95 hover:text-white"
>
  {isFilterOpen ? <X className="h-5 w-5" /> : <Filter className="h-5 w-5" />}
</button>
```

**Стили:**
- Размер: `h-10 w-10`
- Скругление: `rounded-r-xl` (только правые углы)
- Фон: `bg-zinc-900/95 backdrop-blur-xl`
- Граница: `border border-l-0 border-white/[0.12]`
- Иконка меняется: `Filter` (закрыта) → `X` (открыта)

---

### Панель фильтров (открытое состояние)

**Структура:**
```tsx
<div className="h-full overflow-y-auto border-r border-white/[0.08] bg-zinc-900/95 backdrop-blur-xl">
  <div className="p-6">
    {/* Заголовок */}
    <div className="mb-6 flex items-center justify-between">
      <h2 className="text-lg font-semibold text-white">Фильтры</h2>
      {hasActiveFilters && (
        <button onClick={clearFilters} className="text-xs font-medium text-blue-400 hover:text-blue-300">
          Очистить
        </button>
      )}
    </div>
    
    {/* Фильтр по исполнителям */}
    <div className="mb-6">
      {/* ... */}
    </div>
    
    {/* Фильтр по статусам */}
    <div className="mb-6">
      {/* ... */}
    </div>
    
    {/* Фильтр по дедлайнам */}
    <div>
      {/* ... */}
    </div>
  </div>
</div>
```

**Стили панели:**
- Фон: `bg-zinc-900/95 backdrop-blur-xl`
- Граница: `border-r border-white/[0.08]`
- Прокрутка: `overflow-y-auto`
- Отступы контента: `p-6`

---

### 1. Фильтр по исполнителям

**Заголовок:**
```tsx
<div className="mb-3 flex items-center gap-2 text-sm font-medium text-white/60">
  <User className="h-4 w-4" />
  <span>Исполнители</span>
</div>
```

**Список исполнителей:**
```tsx
<div className="space-y-2">
  {projectMembers.map((member) => {
    const stats = getMemberStats(member.id)
    const isSelected = filters.assignees.includes(member.id)
    return (
      <button
        onClick={() => toggleAssigneeFilter(member.id)}
        className={`w-full rounded-lg border p-3 text-left transition-all ${
          isSelected
            ? "border-blue-500/50 bg-blue-500/10"
            : "border-white/[0.08] bg-white/[0.02] hover:bg-white/[0.06]"
        }`}
      >
        <div className="mb-2 flex items-center gap-2">
          {/* Аватар */}
          <div className="h-6 w-6 rounded-full bg-gradient-to-br from-blue-500 to-purple-500">
            {member.avatar_image ? (
              <img className="h-full w-full object-cover" />
            ) : (
              <div className="flex h-full w-full items-center justify-center text-xs font-bold text-white">
                {display_name[0].toUpperCase()}
              </div>
            )}
          </div>
          <span className="text-sm font-medium text-white">
            {member.display_name}
          </span>
        </div>
        {/* Статистика */}
        <div className="flex items-center gap-3 text-xs text-white/50">
          <span>Всего: {stats.total}</span>
          <span className="text-emerald-400">✓ {stats.done}</span>
          <span className="text-blue-400">⟳ {stats.inProgress}</span>
          <span className="text-white/40">○ {stats.todo}</span>
        </div>
      </button>
    )
  })}
</div>
```

**Статистика для каждого исполнителя:**
- **Всего:** общее количество задач
- **✓ Выполнено:** задачи со статусом `completed` (зеленый)
- **⟳ В работе:** задачи со статусом `in_progress` (синий)
- **○ Не начато:** задачи со статусом `todo` (серый)

**Состояния кнопки:**
- **Не выбрано:** `border-white/[0.08] bg-white/[0.02]`
- **Выбрано:** `border-blue-500/50 bg-blue-500/10`

**Функция:** Клик переключает фильтр (добавляет/удаляет из списка)

---

### 2. Фильтр по статусам

**Заголовок:**
```tsx
<div className="mb-3 flex items-center gap-2 text-sm font-medium text-white/60">
  <CheckCircle2 className="h-4 w-4" />
  <span>Статус</span>
</div>
```

**Список статусов:**
```tsx
<div className="space-y-2">
  {/* Не начато */}
  <button
    onClick={() => toggleStatusFilter("todo")}
    className={`flex w-full items-center gap-3 rounded-lg border p-3 transition-all ${
      filters.statuses.includes("todo")
        ? "border-gray-500/50 bg-gray-500/10"
        : "border-white/[0.08] bg-white/[0.02] hover:bg-white/[0.06]"
    }`}
  >
    <Circle className="h-4 w-4 text-gray-400" />
    <span className="text-sm font-medium text-white">Не начато</span>
    <span className="ml-auto text-xs text-white/50">
      {tasks.filter(t => t.status === "todo").length}
    </span>
  </button>
  
  {/* В работе */}
  <button onClick={() => toggleStatusFilter("in_progress")}>
    <PlayCircle className="h-4 w-4 text-blue-400" />
    <span>В работе</span>
    <span className="ml-auto">{count}</span>
  </button>
  
  {/* Выполнено */}
  <button onClick={() => toggleStatusFilter("done")}>
    <CheckCircle2 className="h-4 w-4 text-emerald-400" />
    <span>Выполнено</span>
    <span className="ml-auto">{count}</span>
  </button>
</div>
```

**Статусы:**
1. **Не начато** (`todo`)
   - Иконка: `Circle`, цвет `text-gray-400`
   - Активное состояние: `border-gray-500/50 bg-gray-500/10`

2. **В работе** (`in_progress`)
   - Иконка: `PlayCircle`, цвет `text-blue-400`
   - Активное состояние: `border-blue-500/50 bg-blue-500/10`

3. **Выполнено** (`done`)
   - Иконка: `CheckCircle2`, цвет `text-emerald-400`
   - Активное состояние: `border-emerald-500/50 bg-emerald-500/10`

**Счетчик:** Показывает количество задач с данным статусом

---

### 3. Фильтр по дедлайнам

**Заголовок:**
```tsx
<div className="mb-3 flex items-center gap-2 text-sm font-medium text-white/60">
  <Clock className="h-4 w-4" />
  <span>Дедлайн</span>
</div>
```

**Варианты фильтров:**
```tsx
<div className="space-y-2">
  {/* Просрочено */}
  <button
    onClick={() => setFilters(prev => ({ ...prev, deadlineFilter: "overdue" }))}
    className={`flex w-full items-center gap-3 rounded-lg border p-3 transition-all ${
      filters.deadlineFilter === "overdue"
        ? "border-red-500/50 bg-red-500/10"
        : "border-white/[0.08] bg-white/[0.02] hover:bg-white/[0.06]"
    }`}
  >
    <AlertTriangle className="h-4 w-4 text-red-400" />
    <span className="text-sm font-medium text-white">Просрочено</span>
  </button>
  
  {/* Сегодня */}
  <button onClick={() => setFilters(prev => ({ ...prev, deadlineFilter: "today" }))}>
    <Clock className="h-4 w-4 text-orange-400" />
    <span>Сегодня</span>
  </button>
  
  {/* На неделе */}
  <button onClick={() => setFilters(prev => ({ ...prev, deadlineFilter: "week" }))}>
    <Clock className="h-4 w-4 text-yellow-400" />
    <span>На неделе</span>
  </button>
  
  {/* Все задачи (если фильтр активен) */}
  {filters.deadlineFilter !== "all" && (
    <button onClick={() => setFilters(prev => ({ ...prev, deadlineFilter: "all" }))}>
      Все задачи
    </button>
  )}
</div>
```

**Варианты:**
1. **Просрочено** (`overdue`)
   - Иконка: `AlertTriangle`, цвет `text-red-400`
   - Показывает задачи с дедлайном < текущей даты

2. **Сегодня** (`today`)
   - Иконка: `Clock`, цвет `text-orange-400`
   - Показывает задачи с дедлайном = сегодня

3. **На неделе** (`week`)
   - Иконка: `Clock`, цвет `text-yellow-400`
   - Показывает задачи с дедлайном в течение 7 дней

4. **Все задачи** (`all`)
   - Показывается только если активен другой фильтр
   - Сбрасывает фильтр по дедлайну

---

### Применение фильтров

**Логика фильтрации:**
- Фильтры работают по принципу **И** (AND)
- Задача должна соответствовать **всем** активным фильтрам
- Если фильтр не выбран - он не применяется

**Визуальный эффект:**
- Отфильтрованные задачи: `opacity: 0.25`, `filter: grayscale(0.8)`
- Видимые задачи: `opacity: 1`, `filter: none`
- Анимация: `transition: opacity 0.2s ease, filter 0.2s ease`

---

## Основная область - Канвас задач

### Описание

**Канвас** - это бесконечное пространство для размещения задач. Представляет собой виртуальную область 5000x5000px с возможностью масштабирования и панорамирования.

---

### Структура канваса

```tsx
<div
  ref={canvasRef}
  className="relative flex-1 overflow-hidden"
  style={{
    cursor: isPanning ? "grabbing" : isSpacePressed ? "grab" : "default",
    backgroundImage: `
      linear-gradient(to right, rgba(255, 255, 255, 0.025) 1px, transparent 1px),
      linear-gradient(to bottom, rgba(255, 255, 255, 0.025) 1px, transparent 1px)
    `,
    backgroundSize: `${GRID_SIZE * scale}px ${GRID_SIZE * scale}px`,
    backgroundPosition: `${offset.x}px ${offset.y}px`,
  }}
  onMouseDown={handleMouseDown}
  onDoubleClick={handleCanvasDoubleClick}
  onContextMenu={handleContextMenu}
>
  <div
    data-canvas-content
    className="relative"
    style={{
      transform: `translate(${offset.x}px, ${offset.y}px) scale(${scale})`,
      transformOrigin: "0 0",
      width: `${CANVAS_WIDTH}px`,
      height: `${CANVAS_HEIGHT}px`,
    }}
  >
    {/* SVG для связей */}
    <svg>{drawTaskConnections()}</svg>
    
    {/* Задачи */}
    <div className="relative" style={{ zIndex: 10 }}>
      {tasks.map(task => <TaskNode key={task.id} task={task} />)}
    </div>
  </div>
</div>
```

---

### Сетка (Grid)

**Параметры:**
- Размер ячейки: `GRID_SIZE = 20px`
- Масштабирование: `GRID_SIZE * scale` (изменяется при зуме)
- Цвет линий: `rgba(255, 255, 255, 0.025)` (очень прозрачный белый)
- Толщина: `1px`

**Визуализация:**
- Горизонтальные линии: `linear-gradient(to bottom, ...)`
- Вертикальные линии: `linear-gradient(to right, ...)`
- Позиция: синхронизирована с `offset` (двигается при панорамировании)

**Привязка к сетке:**
- Задачи привязываются к сетке при перемещении
- Функция: `snapToGrid(value) = Math.round(value / GRID_SIZE) * GRID_SIZE`

---

### Связи между задачами (Connections)

**Отображение:**
- SVG элемент поверх канваса
- Z-index: `1` (под задачами)
- Стили: `pointer-events-none` (не блокирует клики)

**Тип связей:**
- **Родитель-потомок:** Линия от нижней части родительской задачи к верхней части дочерней

**Визуализация:**
```tsx
<svg className="pointer-events-none absolute left-0 top-0">
  {tasks
    .filter(task => task.parent_id)
    .map(task => {
      const parent = tasks.find(t => t.id === task.parent_id)
      // Кривая Безье от родителя к потомку
      const path = `M ${startX} ${startY} C ${startX} ${controlY}, ${endX} ${controlY}, ${endX} ${endY}`
      return (
        <g key={`parent-child-${task.id}`}>
          <path d={path} stroke="rgba(255, 255, 255, 0.3)" strokeWidth="2" fill="none" />
          <circle cx={startX} cy={startY} r="4" fill="rgba(255, 255, 255, 0.5)" />
          <circle cx={endX} cy={endY} r="4" fill="rgba(255, 255, 255, 0.5)" />
        </g>
      )
    })}
</svg>
```

**Стили:**
- Линия: `stroke="rgba(255, 255, 255, 0.3)"`, `strokeWidth="2"`
- Точки соединения: `r="4"`, `fill="rgba(255, 255, 255, 0.5)"`
- Кривая: Безье с контрольной точкой посередине

**Анимация при перетаскивании:**
- Связи обновляются в реальном времени
- Используются временные координаты (`draggingPosition`)

---

### Фон канваса

**Градиент:**
```css
bg-gradient-to-br from-neutral-950 via-zinc-950 to-neutral-900
```

**Описание:**
- Диагональный градиент от темно-серого к черному
- Создает глубину и визуальную иерархию

---

## Карточка задачи (Task Node)

### Общее описание

**Карточка задачи** - это основной элемент интерфейса, представляющий одну задачу. Карточки можно перемещать, редактировать, связывать и настраивать.

---

### Структура карточки

```
┌─────────────────────────────────────┐
│ [Название задачи]              [👤] │ ← Заголовок
├─────────────────────────────────────┤
│ СОДЕРЖИМОЕ                           │ ← Содержимое (если есть)
│  [1] Пункт 1                        │
│  [2] Пункт 2                        │
│  [3] Пункт 3                        │
│  [+ Добавить пункт]                │
└─────────────────────────────────────┘
     ↖ (resize handle в правом нижнем углу)
```

---

### Позиционирование

**Абсолютное позиционирование:**
```tsx
<div
  className="absolute"
  style={{
    left: isDragging ? tempPosition.x : task.position_x,
    top: isDragging ? tempPosition.y : task.position_y,
    width: cardSize.width,
    zIndex: isSelected || isInteracting ? 30 : 10,
  }}
>
```

**Координаты:**
- `position_x`, `position_y` - координаты в пространстве канваса
- Привязка к сетке: `snapToGrid()`
- Z-index: выше для выбранных задач

---

### Визуальные стили

#### Обычное состояние

```tsx
style={{
  borderColor: `rgba(${taskColor}, 0.4)`,
  borderWidth: "1.5px",
  boxShadow: `0 4px 16px rgba(0, 0, 0, 0.4)`,
  background: `linear-gradient(135deg, rgba(${taskColor}, 0.1) 0%, rgba(${taskColor}, 0.05) 100%)`,
}}
```

**Характеристики:**
- Граница: `1.5px`, цвет зависит от `taskColor` (40% прозрачности)
- Тень: `0 4px 16px rgba(0, 0, 0, 0.4)`
- Фон: градиент с цветом задачи (10% → 5% прозрачности)
- Скругление: `rounded-2xl`
- Эффект стекла: `backdrop-blur-xl`

---

#### Выбранное/активное состояние

```tsx
style={{
  borderColor: `rgba(${taskColor}, 0.7)`,
  borderWidth: "2px",
  boxShadow: `0 8px 32px rgba(${taskColor}, 0.2), 0 0 0 1px rgba(${taskColor}, 0.3)`,
  background: `linear-gradient(135deg, rgba(${taskColor}, 0.14) 0%, rgba(${taskColor}, 0.08) 100%)`,
}}
```

**Характеристики:**
- Граница: `2px`, более яркая (70% прозрачности)
- Тень: усиленная с цветным свечением
- Фон: более яркий градиент (14% → 8%)
- Z-index: `30` (поверх других задач)

---

### Заголовок карточки

**Структура:**
```tsx
<div className="flex items-center gap-3 p-3 pl-3.5">
  <div className="flex-1">
    <Textarea
      value={localTitle}
      onChange={(e) => setLocalTitle(e.target.value)}
      onBlur={handleTitleBlur}
      placeholder="Название задачи..."
      className="min-h-[36px] resize-none border-0 bg-transparent p-0 text-sm font-semibold leading-relaxed text-white placeholder:text-white/35 focus-visible:ring-0"
    />
  </div>
  
  {/* Кнопка аватара/настроек */}
  <button onClick={() => setIsExpanded(!isExpanded)}>
    {/* Аватар исполнителя */}
  </button>
</div>
```

**Поле названия:**
- Тип: `Textarea` (многострочный текст)
- Стили: прозрачный фон, без границы
- Размер: `text-sm font-semibold`
- Минимальная высота: `min-h-[36px]`
- Автоматическое изменение размера: `fieldSizing: "content"`

**Сохранение:**
- При потере фокуса (`onBlur`) - сохраняется в БД
- Оптимистичное обновление UI

---

### Кнопка аватара/настроек

**Внешний вид:**
```tsx
<button className={`flex h-7 w-7 shrink-0 items-center justify-center rounded-full text-xs font-bold text-white shadow-lg transition-all hover:scale-105 overflow-hidden ${STATUS_RING_COLORS[task.status]}`}>
  {assignedUserProfile?.avatar_image ? (
    <img src={avatar_image} className="h-full w-full object-cover" />
  ) : (
    <span>{display_name[0]?.toUpperCase() || "?"}</span>
  )}
</button>
```

**Стили:**
- Размер: `h-7 w-7`
- Форма: `rounded-full`
- Фон: цвет аватара исполнителя или серый по умолчанию
- Кольцо статуса: `ring-[1.5px]` с цветом статуса
- Hover: `hover:scale-105` (увеличение на 5%)

**Цвета кольца по статусам:**
- `todo`: `ring-gray-400`
- `in_progress`: `ring-amber-500`
- `completed`: `ring-green-500`
- `blocked`: `ring-red-500`

**Функция:**
- Клик открывает/закрывает панель настроек задачи
- Показывает аватар исполнителя или "?" если не назначен

**Tooltip:**
- При наведении показывает имя исполнителя и дедлайн (если есть)

---

### Содержимое задачи (Content Items)

**Условие отображения:** `task.has_content === true` и `contentItems.length > 0`

#### Развернутое состояние

**Структура:**
```tsx
<div className="border-t border-white/[0.08] bg-black/10 p-3 space-y-2">
  {/* Заголовок секции */}
  <div className="flex items-center justify-between mb-2">
    <span className="text-xs font-semibold uppercase tracking-wider text-white/50">
      Содержимое
    </span>
    <button onClick={() => setIsContentCollapsed(true)}>
      <ChevronUp className="h-3.5 w-3.5" />
    </button>
  </div>
  
  {/* Список элементов */}
  <div className="space-y-2 max-h-[320px] overflow-y-auto pr-1">
    {contentItems.map((item, index) => (
      <div key={item.id} className="group relative">
        {/* Кнопка удаления (при hover) */}
        <button className="absolute right-1 top-1 z-10 opacity-0 group-hover:opacity-100">
          <X className="h-3.5 w-3.5" />
        </button>
        
        <div className="flex items-start gap-2">
          {/* Номер/чекбокс */}
          <button
            onClick={() => handleToggleContentComplete(item.id, !item.completed)}
            className={`mt-0.5 flex h-8 w-8 shrink-0 items-center justify-center rounded-lg text-xs font-bold transition-all ${
              item.completed
                ? "bg-emerald-500 text-white shadow-lg shadow-emerald-500/25"
                : "bg-white/[0.08] text-white/60 hover:bg-white/[0.14] border border-white/20"
            }`}
          >
            {index + 1}
          </button>
          
          {/* Текст элемента */}
          <textarea
            value={item.content}
            onChange={(e) => handleUpdateContent(item.id, e.target.value)}
            className={`flex-1 min-h-[32px] resize-none border-0 bg-white/[0.06] px-2.5 py-2 text-xs leading-relaxed text-white placeholder:text-white/30 focus:outline-none focus:ring-1 focus:ring-white/20 rounded-lg transition-all ${
              item.completed ? "line-through opacity-50" : ""
            }`}
            rows={1}
            style={{ fieldSizing: "content" }}
          />
        </div>
      </div>
    ))}
  </div>
  
  {/* Кнопка добавления */}
  <button
    onClick={() => handleAddEmptyContent()}
    className="w-full mt-2 flex items-center justify-center gap-2 rounded-lg bg-blue-500/[0.15] text-blue-400 hover:bg-blue-500/25 transition-all py-2 text-xs font-semibold border border-blue-500/20"
  >
    <Plus className="h-3.5 w-3.5" />
    Добавить пункт
  </button>
</div>
```

**Элементы:**
1. **Номер/чекбокс:**
   - Не выполнен: серый фон, номер пункта
   - Выполнен: зеленый фон, белый текст, тень

2. **Текст элемента:**
   - Редактируемое поле
   - Выполненные: зачеркнуты, прозрачность 50%
   - Автоматическое изменение высоты

3. **Кнопка удаления:**
   - Появляется при hover (`opacity-0 group-hover:opacity-100`)
   - Красный цвет: `text-red-400 hover:text-red-300`

4. **Кнопка добавления:**
   - Синий фон: `bg-blue-500/[0.15]`
   - Иконка: `Plus`

**Максимальная высота:** `max-h-[320px]` с прокруткой

---

#### Свернутое состояние

**Структура:**
```tsx
<div className="border-t border-white/[0.08] bg-black/10 px-3 py-2">
  <button
    onClick={() => setIsContentCollapsed(false)}
    className="flex w-full items-center justify-between text-xs font-semibold text-white/50 hover:text-white/70 transition-colors"
  >
    <span className="uppercase tracking-wider">
      Содержимое ({completed}/{total})
    </span>
    <ChevronDown className="h-3.5 w-3.5" />
  </button>
</div>
```

**Отображает:**
- Название секции: "СОДЕРЖИМОЕ"
- Прогресс: `{completed}/{total}` (выполнено/всего)
- Иконка: `ChevronDown` (указывает на возможность развернуть)

---

### Handle для изменения размера (Resize Handle)

**Расположение:** Правый нижний угол карточки

**Структура:**
```tsx
{canEdit && (
  <div
    onMouseDown={handleResizeMouseDown}
    className="absolute bottom-0 right-0 h-4 w-4 cursor-nwse-resize opacity-0 hover:opacity-100 transition-opacity"
    style={{
      background: `linear-gradient(135deg, transparent 0%, transparent 50%, rgba(${taskColor}, 0.6) 50%, rgba(${taskColor}, 0.8) 100%)`,
      borderBottomRightRadius: "0.75rem",
    }}
  />
)}
```

**Характеристики:**
- Размер: `h-4 w-4`
- Видимость: `opacity-0 hover:opacity-100` (появляется при hover)
- Курсор: `cursor-nwse-resize` (диагональная стрелка)
- Градиент: треугольник в правом нижнем углу
- Цвет: соответствует цвету задачи

**Функция:**
- Перетаскивание изменяет ширину и высоту карточки
- Минимальная ширина: `200px`
- Минимальная высота: `0px`

---

## Панель настроек задачи

### Расположение

**Позиция:** Фиксированная, рядом с выбранной задачей  
**Z-index:** `z-[110]` (поверх всего)  
**Ширина:** `320px` (w-80)

**Логика позиционирования:**
```typescript
// Если справа есть место (≥320px + 10px + 20px)
if (spaceOnRight >= panelWidth + gap + 20) {
  return { left: taskRight + gap, top: taskTop }
} else {
  // Иначе слева от задачи
  return { left: taskLeft - panelWidth - gap, top: taskTop }
}
```

**Отступ от задачи:** `10px` (gap)

---

### Структура панели

```tsx
<div className="fixed z-[110] w-80 rounded-xl border border-white/[0.12] bg-zinc-900/98 shadow-2xl backdrop-blur-2xl">
  {/* Заголовок */}
  <div className="flex items-center justify-between border-b border-white/[0.08] p-3">
    <h3 className="text-xs font-semibold uppercase tracking-wider text-white/60">
      Настройки
    </h3>
    <button onClick={() => setIsExpanded(false)}>
      <X className="h-3.5 w-3.5" />
    </button>
  </div>
  
  {/* Контент */}
  <div className="p-3 space-y-3 max-h-[70vh] overflow-y-auto">
    {/* Даты */}
    <div className="grid grid-cols-2 gap-2 pb-3 border-b border-white/[0.06]">
      {/* Создано */}
      <div>
        <label>Создано</label>
        <div>{created_at}</div>
      </div>
      {/* Дедлайн */}
      <div>
        <label>Дедлайн</label>
        <input type="date" />
      </div>
    </div>
    
    {/* Статус */}
    <div>
      <label>Статус</label>
      <div className="grid grid-cols-2 gap-1.5">
        {/* Кнопки статусов */}
      </div>
    </div>
    
    {/* Исполнитель */}
    <div>
      <label>Исполнитель</label>
      <AssigneeSelector />
    </div>
    
    {/* Цвет */}
    <div>
      <label>Цвет</label>
      <div className="flex flex-wrap gap-1.5">
        {/* Палитра цветов */}
      </div>
    </div>
    
    {/* Удалить */}
    <button className="w-full border border-red-500/30 bg-red-500/[0.12] text-red-400">
      <Trash2 /> Удалить
    </button>
  </div>
</div>
```

**Стили панели:**
- Фон: `bg-zinc-900/98 backdrop-blur-2xl`
- Граница: `border border-white/[0.12]`
- Тень: `shadow-2xl`
- Скругление: `rounded-xl`
- Максимальная высота: `max-h-[70vh]` с прокруткой

---

### 1. Секция дат

**Структура:**
```tsx
<div className="grid grid-cols-2 gap-2 pb-3 border-b border-white/[0.06]">
  {/* Создано */}
  <div>
    <label className="mb-1.5 block text-[10px] font-semibold uppercase tracking-wider text-white/40">
      Создано
    </label>
    <div className="text-xs text-white/60">
      {task.created_at ? new Date(task.created_at).toLocaleDateString("ru-RU") : "—"}
    </div>
  </div>
  
  {/* Дедлайн */}
  <div>
    <label className="mb-1.5 block text-[10px] font-semibold uppercase tracking-wider text-white/40">
      Дедлайн
    </label>
    <input
      type="date"
      value={task.deadline ? new Date(task.deadline).toISOString().split("T")[0] : ""}
      onChange={(e) => {
        const newDate = e.target.value ? new Date(e.target.value).toISOString() : null
        onUpdate(task.id, { deadline: newDate })
      }}
      className="w-full rounded-lg border border-white/[0.08] bg-white/[0.04] px-2 py-1 text-xs text-white/80 focus:border-white/20 focus:outline-none focus:ring-1 focus:ring-white/20"
    />
  </div>
</div>
```

**Поля:**
- **Создано:** Только для чтения, формат `DD.MM.YYYY`
- **Дедлайн:** Редактируемое поле типа `date`

**Стили:**
- Сетка: `grid grid-cols-2 gap-2`
- Разделитель: `border-b border-white/[0.06]`
- Метки: `text-[10px] font-semibold uppercase tracking-wider text-white/40`

---

### 2. Селектор статуса

**Структура:**
```tsx
<div>
  <label className="mb-1.5 block text-[10px] font-semibold uppercase tracking-wider text-white/40">
    Статус
  </label>
  <div className="grid grid-cols-2 gap-1.5">
    {Object.entries(STATUS_LABELS).map(([status, label]) => (
      <button
        onClick={() => handleStatusChange(status)}
        className={`rounded-lg border px-2.5 py-1.5 text-[11px] font-medium transition-all ${
          task.status === status
            ? STATUS_COLORS[status]  // Активный стиль
            : "border-white/[0.08] bg-white/[0.04] text-white/50 hover:bg-white/[0.08] hover:text-white/70"
        }`}
      >
        {label}
      </button>
    ))}
  </div>
</div>
```

**Статусы:**
1. **К выполнению** (`todo`)
   - Активный: `bg-gray-500/20 text-gray-300 border-gray-500/30`

2. **В работе** (`in_progress`)
   - Активный: `bg-amber-500/20 text-amber-300 border-amber-500/30`

3. **Завершено** (`completed`)
   - Активный: `bg-green-500/20 text-green-300 border-green-500/30`

4. **Заблокировано** (`blocked`)
   - Активный: `bg-red-500/20 text-red-300 border-red-500/30`

**Сетка:** `grid grid-cols-2 gap-1.5` (2 колонки)

---

### 3. Селектор исполнителя

**Компонент:** `AssigneeSelector`

**Структура:**
```tsx
<div className="rounded-xl border border-white/[0.12] bg-white/[0.04] p-1.5">
  <div className="flex flex-wrap gap-2">
    {/* Кнопка "Не назначен" */}
    <button className="flex h-7 w-7 items-center justify-center rounded-full border-2">
      <User className="h-3.5 w-3.5" />
    </button>
    
    {/* Аватары участников */}
    {members.map(member => (
      <button
        onClick={() => onAssigneeChange(member.user_id)}
        className={`flex h-7 w-7 items-center justify-center rounded-full text-[10px] font-bold text-white ${
          currentAssigneeId === member.user_id
            ? "ring-2 ring-white/70 ring-offset-2"
            : "opacity-80 hover:opacity-100"
        }`}
        style={{ backgroundColor: member.profile.avatar_color }}
      >
        {member.profile.avatar_image ? (
          <img className="h-full w-full object-cover" />
        ) : (
          <span>{display_name[0]}</span>
        )}
      </button>
    ))}
  </div>
</div>
```

**Функции:**
- Выбор исполнителя из списка участников
- Кнопка "Не назначен" (иконка `User`)
- Выделение текущего исполнителя: `ring-2 ring-white/70`

---

### 4. Палитра цветов

**Структура:**
```tsx
<div>
  <label className="mb-1.5 block text-[10px] font-semibold uppercase tracking-wider text-white/40">
    Цвет
  </label>
  <div className="flex flex-wrap gap-1.5">
    {BASE_COLORS.map((color) => (
      <button
        onClick={() => handleColorChange(color.base)}
        className={`h-[17px] w-[17px] rounded-lg transition-all hover:scale-110 ${
          taskColor === color.base
            ? "ring-2 ring-white/50 ring-offset-2 ring-offset-zinc-900"
            : ""
        }`}
        style={{ backgroundColor: `rgb(${color.base})` }}
      />
    ))}
  </div>
</div>
```

**Цвета:**
- Gray: `107, 114, 128`
- Red: `239, 68, 68`
- Orange: `249, 115, 22`
- Yellow: `234, 179, 8`
- Green: `34, 197, 94`
- Teal: `20, 184, 166`
- Blue: `59, 130, 246`
- Purple: `168, 85, 247`
- Pink: `236, 72, 153`

**Выделение:**
- Активный цвет: `ring-2 ring-white/50 ring-offset-2`
- Hover: `hover:scale-110` (увеличение на 10%)

---

### 5. Кнопка удаления

**Структура:**
```tsx
<button
  onClick={() => setShowDeleteDialog(true)}
  className="flex w-full items-center justify-center gap-2 rounded-lg border border-red-500/30 bg-red-500/[0.12] px-3 py-2 text-xs font-semibold text-red-400 transition-all hover:bg-red-500/20 mt-2"
>
  <Trash2 className="h-3.5 w-3.5" />
  Удалить
</button>
```

**Стили:**
- Фон: `bg-red-500/[0.12]`
- Граница: `border-red-500/30`
- Текст: `text-red-400`
- Hover: `hover:bg-red-500/20`

**Функция:**
- Открывает диалог подтверждения удаления
- После подтверждения - удаляет задачу

---

## Контекстные меню

### Контекстное меню канваса

**Условие:** Правый клик на пустом месте канваса (только для редакторов)

**Расположение:** В позиции курсора мыши

**Структура:**
```tsx
<div
  className="fixed z-[100] min-w-[220px] rounded-xl border border-white/[0.12] bg-zinc-900/98 p-1.5 shadow-2xl backdrop-blur-2xl"
  style={{
    left: contextMenu.x,
    top: contextMenu.y,
  }}
>
  <button
    onClick={() => handleCreateTask(null, x, y, false)}
    className="w-full rounded-lg px-4 py-2.5 text-left text-sm font-medium text-white transition-all hover:bg-white/[0.08]"
  >
    Создать задачу
  </button>
  <button
    onClick={() => handleCreateTask(null, x, y, true)}
    className="w-full rounded-lg px-4 py-2.5 text-left text-sm font-medium text-white transition-all hover:bg-white/[0.08]"
  >
    Создать задачу с содержимым
  </button>
</div>
```

**Опции:**
1. **Создать задачу** - обычная задача без содержимого
2. **Создать задачу с содержимым** - задача с предустановленным чеклистом

**Стили:**
- Фон: `bg-zinc-900/98 backdrop-blur-2xl`
- Граница: `border border-white/[0.12]`
- Тень: `shadow-2xl`
- Минимальная ширина: `220px`

---

### Контекстное меню задачи

**Условие:** Правый клик на карточке задачи (только для редакторов)

**Структура:**
```tsx
<div
  className="fixed z-[100] min-w-[220px] rounded-xl border border-white/[0.12] bg-zinc-900/98 p-1.5 shadow-2xl backdrop-blur-2xl"
  style={{
    left: taskContextMenu.x,
    top: taskContextMenu.y,
  }}
>
  <button
    onClick={() => handleCreateTask(parentId, x, y)}
    className="w-full rounded-lg px-4 py-2.5 text-left text-sm font-medium text-white transition-all hover:bg-white/[0.08]"
  >
    Создать связанную задачу
  </button>
  <button
    onClick={() => handleEnableTaskContent(taskId)}
    className="w-full rounded-lg px-4 py-2.5 text-left text-sm font-medium text-white transition-all hover:bg-white/[0.08]"
  >
    Добавить содержимое
  </button>
</div>
```

**Опции:**
1. **Создать связанную задачу** - создает дочернюю задачу (связь родитель-потомок)
2. **Добавить содержимое** - активирует чеклист для задачи

**Позиция:** Под родительской задачей (y = parent.position_y + 150)

---

## Взаимодействия и жесты

### 1. Перетаскивание задач (Drag & Drop)

**Инициация:**
- Нажатие левой кнопки мыши на карточке задачи
- Курсор меняется на `grab` → `grabbing`

**Процесс:**
- Задача следует за курсором
- Привязка к сетке в реальном времени
- Связи обновляются динамически

**Завершение:**
- Отпускание кнопки мыши
- Сохранение новой позиции в БД
- Обновление координат `position_x`, `position_y`

**Ограничения:**
- Только для редакторов (`canEdit === true`)
- Привязка к сетке: `snapToGrid()`

---

### 2. Панорамирование (Pan)

**Способы:**
1. **Space + мышь:**
   - Зажать `Space`
   - Перетаскивать мышью
   - Курсор: `grab` → `grabbing`

2. **Средняя кнопка мыши:**
   - Зажать среднюю кнопку
   - Перетаскивать
   - Предотвращает прокрутку страницы

3. **Колесо мыши (вертикально):**
   - Прокрутка вверх/вниз перемещает канвас
   - Без `Ctrl` - панорамирование

**Визуальная обратная связь:**
- Курсор меняется на `grab` при зажатом Space
- Сетка движется вместе с канвасом

---

### 3. Масштабирование (Zoom)

**Способы:**
1. **Ctrl + колесо мыши:**
   - `Ctrl + колесо вверх` - увеличение
   - `Ctrl + колесо вниз` - уменьшение
   - Шаг: `±10%` (0.9x или 1.1x)

2. **Кнопки в Header:**
   - `ChevronUp` - увеличение
   - `ChevronDown` - уменьшение
   - Шаг: `±10%`

**Диапазон:**
- Минимум: `30%` (0.3x)
- Максимум: `200%` (2.0x)
- По умолчанию: `100%` (1.0x)

**Эффекты:**
- Сетка масштабируется пропорционально
- Задачи масштабируются вместе с канвасом
- Связи между задачами масштабируются

---

### 4. Создание задачи

**Способы:**
1. **Двойной клик на канвасе:**
   - Создает задачу в позиции клика
   - Без содержимого

2. **Контекстное меню канваса:**
   - "Создать задачу" - без содержимого
   - "Создать задачу с содержимым" - с чеклистом

3. **Контекстное меню задачи:**
   - "Создать связанную задачу" - дочерняя задача

**Позиционирование:**
- Координаты вычисляются относительно канваса
- Учитывается масштаб и смещение
- Привязка к сетке

---

### 5. Редактирование задачи

**Название:**
- Клик на поле названия → редактирование
- `Enter` или потеря фокуса → сохранение
- Автоматическое изменение размера поля

**Содержимое:**
- Клик на тексте элемента → редактирование
- Автоматическое изменение высоты
- Сохранение при потере фокуса

**Настройки:**
- Клик на аватар → открытие панели настроек
- Изменение статуса, исполнителя, цвета, дедлайна
- Сохранение в реальном времени

---

### 6. Изменение размера карточки

**Способ:**
- Hover на правый нижний угол → появляется handle
- Перетаскивание handle → изменение размера
- Минимальная ширина: `200px`

**Визуальная обратная связь:**
- Handle появляется при hover
- Курсор: `cursor-nwse-resize`

---

## Горячие клавиши

### Глобальные

| Клавиша | Действие | Описание |
|---------|----------|----------|
| `Space` | Панорамирование | Зажать + перетаскивать мышью |
| `Ctrl + колесо мыши` | Масштабирование | Увеличение/уменьшение |
| `Escape` | Отмена редактирования | Выход из режима редактирования названия проекта |

### В режиме редактирования названия проекта

| Клавиша | Действие |
|---------|----------|
| `Enter` | Сохранить изменения |
| `Escape` | Отменить редактирование |

---

## Визуальные эффекты и анимации

### Переходы

**Общие:**
- `transition-all duration-200` - для карточек задач
- `transition-all duration-300 ease-out` - для панели фильтров
- `transition-opacity` - для фильтрованных задач

**Hover эффекты:**
- Кнопки: `hover:bg-white/[0.06]` или `hover:bg-white/[0.08]`
- Масштаб: `hover:scale-105` или `hover:scale-110`
- Изменение цвета текста: `hover:text-white`

### Анимации

**Появление элементов:**
- Панель фильтров: плавное расширение/сужение
- Контекстные меню: мгновенное появление
- Панель настроек: плавное появление рядом с задачей

**Состояния загрузки:**
- Спиннер: `animate-spin`
- Пульсация: `animate-pulse`

### Визуальные индикаторы

**Выбранная задача:**
- Усиленная граница (2px вместо 1.5px)
- Цветное свечение тени
- Повышенный z-index

**Фильтрованные задачи:**
- Прозрачность: `opacity: 0.25`
- Эффект: `filter: grayscale(0.8)`
- Плавный переход при изменении фильтров

**Онлайн статус:**
- Зеленый кружок: `bg-green-500`
- Позиция: правый нижний угол аватара
- Размер: `h-2.5 w-2.5`

---

## Состояния элементов

### Состояния карточки задачи

1. **Обычное:**
   - Граница: `1.5px`, `rgba(color, 0.4)`
   - Тень: стандартная
   - Z-index: `10`

2. **Выбранное:**
   - Граница: `2px`, `rgba(color, 0.7)`
   - Тень: усиленная с цветным свечением
   - Z-index: `30`
   - Панель настроек открыта

3. **Активное (взаимодействие):**
   - Временное состояние при клике/фокусе
   - Длительность: `300ms`
   - Те же стили что и выбранное

4. **Перетаскивание:**
   - Курсор: `grabbing`
   - Используются временные координаты
   - Связи обновляются в реальном времени

5. **Фильтрованное (скрыто):**
   - Прозрачность: `0.25`
   - Эффект: `grayscale(0.8)`

---

### Состояния кнопок

1. **Обычное:**
   - Полупрозрачный фон
   - Стандартный цвет текста

2. **Hover:**
   - Усиленный фон
   - Более яркий текст
   - Возможно увеличение (`scale-105`)

3. **Активное/Выбранное:**
   - Цветной фон и граница
   - Яркий текст
   - Кольцо для цветовых кнопок

4. **Disabled:**
   - Прозрачность: `opacity-50`
   - Курсор: `not-allowed`

---

### Состояния панели фильтров

1. **Закрыта:**
   - Ширина: `48px`
   - Видна только кнопка переключения
   - Иконка: `Filter`

2. **Открыта:**
   - Ширина: `320px`
   - Полный контент виден
   - Иконка: `X`
   - Анимация: `transition-all duration-300 ease-out`

---

## Реалтайм синхронизация

### Подписки на изменения

**Задачи:**
- События: `INSERT`, `UPDATE`, `DELETE`
- Таблица: `tasks`
- Фильтр: `project_id = ${projectId}`
- Действия:
  - `INSERT` → добавление задачи в список
  - `UPDATE` → обновление задачи
  - `DELETE` → удаление задачи

**Участники:**
- События: `INSERT`, `UPDATE`, `DELETE`
- Таблица: `project_members`
- Фильтр: `project_id = ${projectId}`
- Действие: обновление списка участников

**Онлайн статус:**
- События: `INSERT`, `UPDATE`
- Таблица: `online_users`
- Фильтр: `project_id = ${projectId}`
- Действие: обновление онлайн-статуса

### Обновление присутствия

**Интервал:** Каждые 30 секунд  
**Действие:** Обновление `last_seen` в таблице `online_users`  
**Критерий онлайн:** Активность в течение последних 5 минут

---

## Заключение

Эта инструкция описывает полную структуру и функциональность страницы доски задач. Все элементы интерфейса, взаимодействия, визуальные эффекты и состояния подробно документированы для точного воссоздания интерфейса в новом проекте.

**Ключевые моменты для реализации:**
- ✅ Бесконечный канвас с масштабированием и панорамированием
- ✅ Система фильтрации задач
- ✅ Интерактивные карточки задач с перетаскиванием
- ✅ Панель настроек задачи
- ✅ Контекстные меню
- ✅ Реалтайм синхронизация
- ✅ Визуальные эффекты и анимации
- ✅ Горячие клавиши
