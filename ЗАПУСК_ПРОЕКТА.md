# ✅ Зависимости установлены!

## 🚀 Запуск проекта

Проект запущен! Откройте в браузере:

- **Frontend**: http://localhost:3000
- **Backend API**: http://localhost:3001

## 📝 Что делать дальше:

1. **Откройте браузер** и перейдите на http://localhost:3000
2. **Зарегистрируйте первого пользователя** на странице регистрации
3. **Создайте проект** и начните добавлять задачи

## 🔄 Как запустить проект в следующий раз:

### Вариант 1: Двойной клик на `run.bat`

### Вариант 2: Через командную строку
```cmd
cd c:\Users\RAZER\Desktop\task-manager
npm run dev:all
```

### Вариант 3: Отдельно (в двух терминалах)

**Терминал 1 (Backend):**
```cmd
cd c:\Users\RAZER\Desktop\task-manager\backend
npm run dev
```

**Терминал 2 (Frontend):**
```cmd
cd c:\Users\RAZER\Desktop\task-manager
npm run dev
```

## ⚠️ Если что-то не работает:

1. **Проверьте, что порты свободны:**
   - Порт 3000 должен быть свободен для Frontend
   - Порт 3001 должен быть свободен для Backend

2. **Проверьте подключение к базе данных:**
   - База данных Neon должна быть активна
   - Строка подключения в `backend/.env` должна быть правильной

3. **Перезапустите серверы:**
   - Закройте все терминалы
   - Запустите снова через `run.bat` или `npm run dev:all`

## 📁 Структура проекта:

```
task-manager/
├── app/              # Next.js страницы
├── components/       # React компоненты
├── lib/              # Утилиты и API клиент
├── backend/          # Backend API (Express)
└── node_modules/     # Зависимости (установлены ✅)
```

## 🎉 Готово к работе!

Проект полностью настроен и готов к использованию.
